apiVersion: v1
kind: Service
metadata:
  name: monitoring-dashboard-service
spec:
  type: NodePort
  selector:
    app: monitoring-dashboard
  ports:
  - protocol: TCP
    port: 5000
    targetPort: 5000
    nodePort: 30000
